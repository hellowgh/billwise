{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "baseUrl": ".", // 必须有："paths"相对于此
    "outDir": "dist",
    "paths": {
      "@billwise/db": ["../db/src"],
      "@billwise/trpc": ["./src"]
    }
  },
  "include": ["src"],
  "references": [
    { "path": "../db" } // ✅ 必须引用 db
  ]
}
